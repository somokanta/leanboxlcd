(function ($) {
  Drupal.behaviors.hours = {
    attach: function (context, settings) {
        alert('hi');
        console.log('hi');
        //$(window).load(function(){ alert('hi'); });
    }
  };
})(jQuery);
